\documentclass[11pt,oneside]{article}
\usepackage{geometry}
\geometry{letterpaper}
\usepackage{amssymb}
\usepackage{amsmath}
\usepackage{mathpazo,euler}
\usepackage{hyperref}

\title{Sylow Solver: Haskell Implementation}
\author{Dean Menezes}
\date{}

\begin{document}
\maketitle

This document provides detailed documentation for the Haskell implementation of the automated Sylow theorem prover. For a general overview of the theoretical framework and techniques, please refer to \href{readme.tex}{readme.tex}.

\section{Overview}
The Haskell implementation is a port of the Sylow solver framework,
leveraging Haskell's strong type system and functional paradigms to
provide a robust and extensible theorem prover. It automates common
reasoning patterns that show no simple group of a given order can
exist, encoding techniques as rewrite rules and searching for a contradiction.

\section{Module Map}
The code is split into focused modules:
\begin{center}
\begin{tabular}{ll}
\texttt{src/Core.hs} & Core data types: \texttt{Fact}, \texttt{Arg}, labels, theorems \\
\texttt{src/Predicates.hs} & Fact constructors for group-theoretic predicates \\
\texttt{src/Env.hs} & Proof environment and pure state transitions \\
\texttt{src/EnvPrint.hs} & Printing of proof traces (IO only) \\
\texttt{src/Auto.hs} & Proof search loop and template matching \\
\texttt{src/Theorems.hs} & Sylow techniques as theorems and rules \\
\texttt{src/NumberTheory.hs} & Primes, divisors, and factorization helpers \\

\texttt{app/Main.hs} & CLI entry point (interactive and batch) \\
\texttt{src/ConclusionDSL.hs} & A DSL for defining conclusions in a more structured way. \\
\texttt{src/DomainTypes.hs} & Defines core data types for the problem domain. \\
\texttt{src/IncrementalMatching.hs} & Logic for incremental matching of facts to theorem premises. \\
\texttt{src/Memoization.hs} & Implements memoization strategies for performance optimization. \\
\texttt{src/ProofMonad.hs} & Monadic structures for managing proof state and operations. \\
\texttt{src/ProofTrace.hs} & Functionality for recording and replaying proof steps. \\
\texttt{src/Unification.hs} & Handles unification of terms during theorem application. \\
\multicolumn{2}{c}{\textbf{Environment Modules}} \\
\texttt{src/Environment/Accessors.hs} & Provides accessors for environment data structures. \\

\texttt{src/Environment/FactsMonadic.hs} & Monadic operations related to managing facts. \\
\texttt{src/Environment/Goals.hs} & Manages proof goals. \\
\texttt{src/Environment/Labels.hs} & Handles labeling of facts and disjunctions. \\
\texttt{src/Environment/Lenses.hs} & Defines lenses for ergonomic access to environment state. \\
\texttt{src/Environment/Symbols.hs} & Manages symbols used in the proof. \\
\texttt{src/Environment/Types.hs} & Defines types specific to the proof environment. \\
\texttt{src/Environment/Variables.hs} & Manages variables in the proof context.
\end{tabular}
\end{center}

\section{Building and Running the Haskell Solver}

This project uses `cabal` for build and dependency management.

\subsection{Prerequisites}
\begin{itemize}
    \item GHC (Glasgow Haskell Compiler)
    \item Cabal (Haskell Cabal Build Tool)
\end{itemize}
These can typically be installed via `ghcup` (https://www.haskell.org/ghcup/).

\subsection{Building the Project}
Navigate to the project root directory and run the following command:
\begin{verbatim}
cabal build
\end{verbatim}
This will compile the project and its dependencies.

\subsection{Running the Executable}
After building, you can run the `sylow-solver` executable.

\subsubsection*{Interactive Mode}
To run the solver in interactive mode, simply execute the compiled program without any arguments:
\begin{verbatim}
cabal run sylow-solver
\end{verbatim}
The program will prompt you to enter group orders.

\subsubsection*{Batch Mode (with arguments)}
You can also provide group orders as command-line arguments using the `--order` flag:
\begin{verbatim}
cabal run sylow-solver -- --order 12 --order 24
\end{verbatim}
This will run the solver for orders 12 and 24 non-interactively.

\section{Facts, Arguments, and Matching}
Facts are predicate names with ordered arguments. Arguments are typed
to make matching explicit:
\begin{itemize}
\item \texttt{Sym s}: a concrete symbol \texttt{s} (treated literally)
\item \texttt{Var v}: a template variable \texttt{v} (can match anything)
\item \texttt{Exact e}: a literal that must match \texttt{e} (e.g. \texttt{1})
\item \texttt{Fresh f}: a new symbol to be generated by the environment
\item \texttt{Num i}: a numeric literal \texttt{i} for efficient number handling
\end{itemize}

Template matching unifies \texttt{Var} and \texttt{Fresh} with concrete
symbols and requires \texttt{Exact} to match literally. After a theorem
fires, any \texttt{Fresh} arguments are replaced with globally fresh
symbols so that new objects do not collide with existing names.

\section{Disjunction Handling}
Disjunctions represent logical \textit{or} at the fact level. When a
disjunction is produced, each branch is added as a fact, and each such
fact records which branch it depends on. A goal is considered proven
only if every full assignment of disjunction branches is covered by a
goal derivation. This is implemented by tracking disjunction ancestry
sets and checking coverage over the Cartesian product of branches.

\section{Theorem Set}
The technique library mirrors standard Sylow-based reasoning:
\begin{itemize}
\item Sylow subgroup counts and existence.
\item Normality from a unique Sylow subgroup.
\item Embedding into alternating groups and index arguments.
\item Counting arguments for elements of $p$-power order.
\item Normalizer-of-intersection arguments and related constraints.
\end{itemize}
Each technique is a theorem object that matches a template and yields
new facts or disjunctions, allowing the proof engine to chain them
automatically.

\section{Worked Example (Order 12)}
Assume a simple group $G$ of order $12$. The prover:
\begin{enumerate}
\item Applies Sylow to derive possible counts of Sylow $2$- and
  $3$-subgroups.
\item If a Sylow count is $1$, it derives that the subgroup is normal
  and hence $G$ is not simple.
\item In remaining branches, it uses counting arguments to show the
  number of elements of $2$- and $3$-power order exceeds $12$.
\end{enumerate}
In each branch, a contradiction is derived, so the goal \texttt{false}
is proven.

\section{Limitations and Scope}
This system is intentionally specialized. It is not a full first-order
theorem prover, and it does not encode group axioms. The search is
brute-force over the technique library, so disjunctions can cause
combinatorial blowups for large orders. Nevertheless, for many orders
the technique set is sufficient and produces short, readable proofs.

\section{References}
Useful references for the underlying group theory include standard
texts such as:
\begin{itemize}
\item Dummit and Foote, \textit{Abstract Algebra}
\item Isaacs, \textit{Finite Group Theory}
\end{itemize}

\end{document}
